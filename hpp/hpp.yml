---
- name: Install MariaDB, Nginx and PHP-FPM
  hosts: hpp-server
  become_user: root
  # remote_user: root
  # remote_user: user
  become: yes
  
  roles:
  - common
  - mariadb
  - httpd
  - php-fpm
